[project]
name = "ryo-arena"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.3.0",
    "httpx>=0.28.1",
    "pydantic>=2",
    "typer>=0.27.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "respx>=0.23.1",
]
