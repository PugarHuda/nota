# Weekly bump proposals for the SHA-pinned actions and the Python dependencies (uv.lock). Each arrives as a
# pull request, so the test workflow runs on it before anything merges.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: weekly
